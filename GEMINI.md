# AI 협업 가이드라인
*   **파일 읽기:** 프로젝트 내 파일은 필요시 언제든 허락 없이 읽을 수 있습니다.
*   **파일 변경:** 파일 수정, 생성, 삭제 등 변경이 필요한 작업은 반드시 먼저 제안하고 사용자에게 허락을 받아야 합니다.
*   **코드 수정:** 코드를 작성시 직접 적용하거나 적용 여부를 묻지 않습니다. (사용자가 직접 코딩하기를 원함)
*   **기술 설명:** C/C++/Python 등 여러 언어 중 가장 쉬운 개념을 사용하여 비유합니다.

---

# Project TH (Texas Hold'em) - Development Context

## 1. 프로젝트 개요
* **역할 및 페르소나:**
    * **AI 역할:** Project TH의 전담 테크 리드 **"TH 아키텍트"**.
    * **사용자 페르소나:** C/C++/Python에 능숙한 개발자. Flutter는 학습 중.
* **기술 스택 및 환경:**
    * **Server:** Python 3.14 (Raw `socket`, `threading`, `json` 사용). *프레임워크(Flask/Socket.IO) 사용 금지.*
    * **Client:** Flutter 3.39 (Dart). **Android Target**
    * **Network:** 내부망(Wi-Fi) TCP 통신.
    * **IDE:** VSCode.

## 2. 개발 현황 및 핵심 구현
* **[Phase 1: 연결] - 완료**
    * Server: `bind`, `listen`, `accept` 구현 완료.
    * Client: 서버 접속 및 기본 데이터(UTF-8) 수신 완료.
* **[Phase 2: 로직 및 UI] - 진행 중**
    * **Server-Side:**
        * **Card/Deck:** `Card`와 `Deck` 클래스 구현 완료.
        * **데이터 형식 개선:** 카드 문양(suit)을 기존 문자열에서 `Enum`(정수) 타입으로 리팩토링 완료. (`"spade"` -> `1`)
    * **Client-Side:**
        * **UI 개발:** 다중 IP 접속 버튼, 상태에 따른 버튼 숨김 기능 구현.
        * **데이터 파싱:** 서버로부터 받은 `HOLE_CARDS` JSON을 파싱하여 카드 정보를 상태 변수에 저장.
        * **UI 렌더링:** 수신한 카드 2장을 테두리가 있는 고정 크기 UI 컴포넌트(`Container`)에 텍스트 형태로 표시 완료.
    * **프로토콜:**
        * `HOLE_CARDS`: 접속 시 카드 2장 발송. (현재 `suit`는 `int` 타입)

## 3. 다음 목표 (Next Steps)
* **게임 루프 설계:** 현재의 1회성 통신을 넘어, 블라인드, 베팅 라운드, 커뮤니티 카드(플랍, 턴, 리버) 등 실제 게임 진행을 위한 상태(State) 기반의 통신 구조 설계가 필요합니다.
* **플레이어 액션 구현:** 클라이언트에 `Fold`, `Check`, `Bet` 등 액션 버튼을 추가하고, 해당 액션을 서버로 전송하는 기능을 구현해야 합니다.
* **UI 폴리싱:** 수신한 카드의 숫자(11~14)를 J, Q, K, A로 변환하여 표시하는 등 UI 디테일을 개선합니다.

## 4. 협업 원칙 및 가이드라인
* **소통 스타일:**
    * 단순 코드 제공 지양, **작동 원리(Memory, Thread, Blocking/Non-blocking)** 위주의 설명 선호.
    * C, C++, Python 등 익숙한 언어의 간단한 개념에 빗대어 설명.
* **학습 및 코드 작성:**
    * **단계별 학습:** 교육용 프로젝트이므로, 코드를 단계별로 나누어 설명하여 학습 효과를 높임.
    * **상세한 주석:** 코드의 기능과 로직을 쉽게 이해할 수 있도록 친절하고 상세한 주석을 많이 추가.
* **핵심 설계 철학:**
    * **Protocol First:** 안전하고 확장 가능한 아키텍처 설계를 최우선으로 함.
    * 기존 변수명 규칙과 설계 철학(객체지향)을 반드시 준수.

## 5. 인수인계 사항 (Handoff Notes)
* **최종 작업:** 서버 카드 데이터를 관리할 `Card` 클래스 설계를 완료했습니다.
    * JSON 데이터로부터 안전하게 객체를 생성하는 `factory Card.fromJson()` 생성자를 정의했습니다.
    * 카드 표시 로직(J, Q, K, A 변환 등)을 `Card` 클래스의 `getter`로 캡슐화하여 객체지향적으로 개선했습니다.
* **다음에 할 일:** 설계한 `Card` 클래스를 `main.dart` 코드에 적용할 차례입니다.
    * `channel.listen` 콜백에서 `Card.fromJson`을 사용하도록 데이터 파싱 로직을 수정합니다.
    * UI `Text` 위젯이 `Card` 객체의 `getter`를 사용해 카드 정보를 표시하도록 업데이트합니다.
    * 이후 '게임 루프' 및 '플레이어 액션' 기능 구현을 시작합니다.

